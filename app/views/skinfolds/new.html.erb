<% content_for :page_title, "Nova Dobra Cutânea - #{@client.name}" %>

<div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- Breadcrumb -->
  <nav class="mb-6">
    <ol class="flex items-center space-x-2 text-sm text-gray-500">
      <li><%= link_to "Dashboard", root_path, class: "hover:text-gray-700" %></li>
      <li><span class="mx-2">/</span></li>
      <li><%= link_to "Clientes", clients_path, class: "hover:text-gray-700" %></li>
      <li><span class="mx-2">/</span></li>
      <li><%= link_to @client.name, client_path(@client), class: "hover:text-gray-700" %></li>
      <li><span class="mx-2">/</span></li>
      <li class="text-gray-900 font-medium">Nova Dobra Cutânea</li>
    </ol>
  </nav>

  <!-- Header -->
  <div class="bg-white shadow-lg rounded-lg p-8 mb-8">
    <div class="text-center">
      <h1 class="text-3xl font-bold text-gray-900 mb-2">Nova Dobra Cutânea</h1>
      <p class="text-xl text-purple-600 font-semibold"><%= @client.name %></p>
      <p class="text-gray-600 mt-2">Registre as medidas de dobras cutâneas do cliente</p>
      
      <!-- Miniguia visual -->
      <div class="mt-6 bg-gradient-to-r from-purple-50 to-pink-50 rounded-lg p-4 max-w-2xl mx-auto">
        <h3 class="text-sm font-semibold text-purple-800 mb-2">💡 Dica de Medição</h3>
        <p class="text-xs text-purple-700">
          As dobras cutâneas devem ser medidas em milímetros (mm) usando um adipômetro. Meça sempre no lado direito do corpo, exceto quando especificado.
        </p>
      </div>
    </div>
  </div>

  <%= render 'form' %>
</div>